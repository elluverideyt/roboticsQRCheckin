<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/css/style.css">
  <title>QR Code</title>
</head>
<body>
  <div class="qr-container">
    <% if (svg) { %>
      <div class="qr-code"><%- svg %></div>
    <% } else { %>
      <p>No QR code yet. Set one on the <a href="/admin">admin page</a>.</p>
    <% } %>
  </div>
</body>
</html>
